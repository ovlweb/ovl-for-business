// Runtime configuration. The Docker image rewrites this file from environment variables.
// apiUrl: "" means "same origin" (the reverse proxy routes /api to the server).
window.__OVL_CONFIG__ = { apiUrl: '' };
